-1-
Основное правило проекта
Если функция оперирует входящими данными ввиде Атомов, то возвращать должна тоже атом
    Func(Atom) => Atom
Если функция принимает и Атомы и JsValue то она вправе вернуть не Атом
    Func(Atom, JsValue) => Atom | JsValue

-2-
Переписать VFunc аналогично Mathan через паттерн контекст байндер. (Также подумать наилучший ли это паттерн для такого)

-3-
