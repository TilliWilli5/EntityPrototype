/**
 * Сет это часть enum'a
 * например var colorSubSet = ["red", "green"]
 * Set должен ссылаться на свой основной enumeration: set(enum)
 * это массив, который содержит значения только из enum'a
 * @module
 */