-1-
Atom = value
Атом равен своему значению
-2-
Атом существует только в рантайме
-3-
Сложность приложения (инфраструктуры приложений оценивается) по количеству эмиттированных Атомов

-4-
Для создания атома необходимо только jsValue. Таким образом Атом являеться однонаправленой функцией проекции из UserSpace (javascript code) -> Medium.Runtime

-5-
substance не коем образом не должен принимать параметров в конструкторе и не должен ничего знать о jsValue

-6-
Перенести ValidateDefaultJsValue в статические методы класса
ValidateDefaultJsValue => static ValidateDefaultJsValue

-7-
Добавить метод ForceConvert for Atom. Смысл в том что:
jsValue приходящее из UserSpace формы будет вначале проверяться на валидность с помощью IsAcceptableJsValue а потом конвериться с помощью метода ForceConvert

-8-
